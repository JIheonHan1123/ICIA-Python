# Jan.12 #######################################################################################
# 파이썬 타입: int, float, str, bool, tuple, list, dictionary, set

# 타입은 어떻게 만드나?
# 파이썬으로 타입 자체를 만들 수 있다. -> 사용자 정의 타입: class (자바할 때 자세하게 설명할거임)
# cf) C언어는 C언어로 만들었다.

# 내가 만든 type인 class의 변수를 object(객체)라고 한다.

# 파이썬의 모든 타입은 클래스다
# 파이썬의 모든 변수는 객체다
# 객체에 소속된 함수를 메소드라고 한다
str = 'hello'
print(type(str))
print(len(str))
